# Whop API Configuration
WHOP_API_KEY=your_whop_api_key_here
WHOP_APP_CLIENT_ID=your_whop_app_client_id_here
WHOP_APP_CLIENT_SECRET=your_whop_app_client_secret_here
NEXT_PUBLIC_WHOP_APP_ID=your_whop_app_id_here

# Whop Subscription Configuration
# Plan ID for your subscription product
WHOP_PLAN_ID=plan_xxxxxxxxxxxxx
# Access Pass ID (product ID) for the subscription
NEXT_PUBLIC_ACCESS_PASS_ID=pass_xxxxxxxxxxxxx
# Webhook secret for verifying webhook signatures from Whop (for app subscriptions)
WHOP_WEBHOOK_SECRET=whsec_xxxxxxxxxxxxx
# Webhook secret for business events (customer's business data updates)
WHOP_BUSINESS_WEBHOOK_SECRET=whsec_xxxxxxxxxxxxx

# MongoDB Connection
MONGO_URI=mongodb://localhost:27017/your_database_name
# Or for MongoDB Atlas:
# MONGO_URI=mongodb+srv://username:password@cluster.mongodb.net/database_name

# Application Configuration
NEXT_PUBLIC_APP_URL=http://localhost:3000
# For production:
# NEXT_PUBLIC_APP_URL=https://yourdomain.com

# Cron Job Configuration
# Optional: Secret token for authenticating cron job requests
# Generate a random string for production (recommended for security)
# Example: openssl rand -hex 32
CRON_SECRET=your_secret_token_here

# Note: Companies are auto-registered when users first visit their dashboard
# No need to manually configure company IDs - they're stored in MongoDB automatically!

# Subscription System:
# 1. Create a product and plan in Whop dashboard
# 2. Copy the Plan ID to WHOP_PLAN_ID
# 3. Copy the Access Pass ID to NEXT_PUBLIC_ACCESS_PASS_ID
# 4. Configure webhook endpoint in Whop: https://yourdomain.com/api/webhooks/whop
# 5. Copy webhook secret to WHOP_WEBHOOK_SECRET
# 6. The system will automatically handle trial periods and subscription management
